@model IEnumerable<WebHelperGrid.Models.Urun>

<h2>UrunListesi</h2>

@{
    var grid = new WebGrid(Model);
}

<div>
    @grid.GetHtml(
    columns: grid.Columns(

    grid.Column("Ad", "Ürün Adı"),
    grid.Column("Fiyat", "Ürün Fiyatı"),
    grid.Column("Adet", "Ürün Sayısı")
    ))
</div>
